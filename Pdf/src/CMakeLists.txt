block()
file(GLOB PROJS *)
foreach(ITEM IN LISTS PROJS)
cmake_path(APPEND CUR_CMAKEFILE_PATH ${ITEM} CMakeLists.txt)
if(EXISTS ${CUR_CMAKEFILE_PATH})
add_subdirectory(${ITEM})
endif()
endforeach()
endblock()